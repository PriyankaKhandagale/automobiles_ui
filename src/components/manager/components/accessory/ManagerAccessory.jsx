import React from 'react';
import { Outlet } from 'react-router-dom';

const ManagerAccessory = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
}

export default ManagerAccessory;

