import React from 'react';
import { Route, Routes, Outlet } from 'react-router-dom';

const Admin = () => {
    return (
        <>
            {/* Admin Component */}
            <Outlet />
        </>
    );
}

export default Admin;
